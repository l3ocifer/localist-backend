# ==============================================
# Localist Backend - Environment Configuration
# ==============================================

# Server Configuration
NODE_ENV=development
PORT=5000
API_URL=http://localhost:5000

# Database Configuration (PostgreSQL)
DATABASE_URL=postgresql://localist:localist123@postgres:5432/localist
DB_HOST=postgres
DB_PORT=5432
DB_NAME=localist
DB_USER=localist
DB_PASSWORD=localist123

# Redis Cache Configuration
REDIS_URL=redis://redis:6379
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# MinIO Object Storage (S3-compatible)
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_USE_SSL=false
MINIO_BUCKET_NAME=localist-uploads

# JWT Authentication
# Generate secure secrets with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d
REFRESH_TOKEN_SECRET=your-super-secret-refresh-token-key-change-this

# CORS Configuration
FRONTEND_URL=http://localhost:3000
CORS_ORIGIN=http://localhost:3000

# External APIs

# OpenAI (for AI recommendations and features)
# Get key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Google Maps (for geocoding and location services)
# Get key from: https://console.cloud.google.com/
GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# Email Configuration (Optional - for production)
# For Gmail, use App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=noreply@localist.com

# Logging
LOG_LEVEL=debug
LOG_FILE=logs/app.log

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Session Configuration
SESSION_SECRET=your-session-secret-key-change-this
SESSION_EXPIRES_IN=86400

# Feature Flags (Optional)
ENABLE_AI_RECOMMENDATIONS=true
ENABLE_EMAIL_NOTIFICATIONS=false
ENABLE_SOCIAL_AUTH=false

# Sentry Error Tracking (Optional - for production)
# Get DSN from: https://sentry.io/
# SENTRY_DSN=your-sentry-dsn-here
# SENTRY_ENVIRONMENT=development

# ==============================================
# Notes:
# ==============================================
# 1. Copy this file to .env and fill in your values
# 2. NEVER commit .env file to version control
# 3. Generate strong secrets for production
# 4. Update API keys with your actual keys
# 5. For Docker: use service names (postgres, redis, minio)
# 6. For local: use localhost
# ==============================================
