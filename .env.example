# Application Configuration
NODE_ENV=development
PORT=3002

# Database Configuration (PostgreSQL)
DATABASE_URL=postgresql://postgres:localist_dev_password@localhost:5432/localist
DB_HOST=localhost
DB_PORT=5432
DB_NAME=localist
DB_USER=postgres
DB_PASSWORD=localist_dev_password

# Redis Configuration
REDIS_URL=redis://:localist_redis_password@localhost:6379
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=localist_redis_password

# JWT Secrets (Generate with: openssl rand -base64 32)
JWT_SECRET=dev_jwt_secret_please_change_in_production_123456
REFRESH_TOKEN_SECRET=dev_refresh_secret_please_change_in_production_123456

# CORS Configuration
FRONTEND_URL=http://localhost:3005
CORS_ORIGIN=http://localhost:3005

# Logging
LOG_LEVEL=debug

# Email (Optional - for production)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-password
# EMAIL_FROM=noreply@localist.com
